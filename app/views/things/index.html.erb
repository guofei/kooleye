<div class="row">

  <% @things.each do |thing| %>
    <div class="col-xs-12 col-sm-6">
      <div class="panel panel-default">
	<div class="panel-body">
	  <%= link_to thing_path(thing), class: "thumbnail" do image_tag thing.images.first.file.normal.url end -%>
	  <p><%= thing.name -%> | <%= thing.summary -%></p>
	</div>
      </div>
    </div>
  <% end %>

</div>
