<% if @comment -%>
  $("#comments").append('<%= j(render partial: 'comments/comment', locals: {comment: @comment}) %>');
<% else -%>
  alert("Captcha入力が間違っています。もう一回入力してください");
<% end %>

if($(".recaptcha-reload")[0]){
  Recaptcha.reload();
}
